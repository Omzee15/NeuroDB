in the query results coloumns header, make the header be 100 % not transparent 

also make the section of the query results and query writing section partition to be draggable

and also in the pop over  of the query results make the pop over to have a copy button as well, and on clicking that it will copy the entire content of the cell. 


